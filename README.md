#bathymetry

### Installation
**Install Gulp**
`npm install gulp --global`

**Install Dependencies**
`cd application && npm install`
*OR*
`cd application && yarn`

**Build**
`gulp build && gulp watch`
